from collections import deque
from pathlib import Path
import numpy as np
import polars as pl

to_list = list
to_set = set
to_tuple = tuple
to_deque = deque
to_dict = dict
to_str = str
to_int = int
to_float = float
to_path = Path
to_array = np.array
to_series = pl.Series
to_dataframe = pl.DataFrame
